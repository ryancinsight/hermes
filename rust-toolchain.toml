# Pinned toolchain for reproducible builds and CI.
# The workspace `rust-version` remains the 1.95 MSRV contract; ordinary CI uses
# this newer pinned toolchain for reproducible development builds.
[toolchain]
channel = "1.97.0"
components = ["rustfmt", "clippy"]
