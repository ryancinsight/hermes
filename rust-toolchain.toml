# Pinned toolchain for reproducible builds and CI.
# MSRV (workspace `rust-version`) matches this pin; verified empirically by
# building and testing the full workspace on 1.95.0.
[toolchain]
channel = "1.97.0"
components = ["rustfmt", "clippy"]
